this example shows how to create init parameters for the filter.
only filter can access its init parameters,not the servlet,therefore its printed in browser as:

value = filter example
in servlet , value = null 